//用筛选法求100以内的素数
#include<stdio.h>
int main() {
int i, j, k = 0, a[100];
for (i = 0; i < 100; i++)
{
	a[i] = i + 1;    //赋100个元素值
}
a[0] = 0;		
for (i = 0; i < 99;i++) {
	for (j = i + 1; j < 100;j++)
	{
           if (a[i]!=0&&a[j]!=0) {       //排除a【0】=0；即1的情况
		   
		if (a[j]%a[i]==0) {
		        a[j] = 0;                    //将数组元素为合数的赋值为0

				}
			
			  }

		     }
	}
	for (k = 1; k < 99;k++) {
		if (a[k]!=0) {
			printf("%d ",a[k]);//将不是0的全输出
			
		}
	}

